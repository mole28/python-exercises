# Temperature Converter

Python script to convert Fahrenheit to Celsius with integrated logging.

## Features
- Handles inputs via `try-except` blocks.
- Absolute zero logic check.
- Multi-level file logging (`logging`).

## Structure
- `main.py`: Entry point.
- `ex_loggin.py`: Core logic and config.

## Run
`python main.py`