# User Data Manager

Python script to fetch, validate, and store user data.

## Features
- Fetch users from API (`httpx`).
- Validate data models (`Pydantic`).
- Save and Load data to/from JSON files.

## Setup
`pip install -r requirements.txt`

## Run
`python main.py`